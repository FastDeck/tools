# FastDeck Tools — Agent Instructions

## Session Bootstrap

At the start of every session in this project, you MUST:

1. Read all skill files in `.antigravity/skills/`:
   ```bash
   ls .antigravity/skills/
   ```
2. Load each skill file using `view_file` or `read` before doing any work.
3. Apply all skills for the entire session — no exceptions.

## Current Skills

- `.antigravity/skills/caveman.md` — Communication style (apply always)
- `.antigravity/skills/commit.md` — Commit & branching workflow (apply on every commit)

## Project

FastDeck Tools monorepo. See `CONTEXT.md` for full project context.
